<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_delete_unlinked_bill_payments" model="ir.actions.server">
        <field name="name">Delete Unlinked Bill Payments</field>
        <field name="model_id" ref="account.model_account_payment"/>
        <field name="binding_model_id" ref="account.model_account_payment"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">action = records.action_delete_unlinked_bill_payments()</field>
    </record>

    <record id="action_delete_all_unlinked_bill_payments" model="ir.actions.server">
        <field name="name">Delete ALL Unlinked Bill Payments</field>
        <field name="model_id" ref="account.model_account_payment"/>
        <field name="binding_model_id" ref="account.model_account_payment"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = model.action_delete_all_unlinked_bill_payments()</field>
    </record>

    <record id="view_account_payment_form_custom_bill_receive_cleanup" model="ir.ui.view">
        <field name="name">account.payment.form.custom.bill.receive.cleanup</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_delete_unlinked_bill_payments"
                        type="object"
                        string="Delete Unlinked Bill Payments"
                        class="btn-secondary"
                        confirm="This will delete only the selected payment if it is an unlinked vendor bill payment. Continue?"/>
            </xpath>
        </field>
    </record>
</odoo>
